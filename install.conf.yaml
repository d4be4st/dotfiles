- defaults:
    link:
      relink: true
      create: true

- clean:
  - "~"
  - "~/.config/nvim"
  - "~/.config/alacritty"
  - "~/.ssh"
  - "~/.rbenv"

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [rm -rf .git/hooks]
  - [rm ~/.zshrc]
  - [op get document zshenv --output zshenv]
  - [op get document id_rsa --output ssh/id_rsa]
  - [op get document id_rsa.pub --output ssh/id_rsa.pub]
  - [op get document ssh_config --output ssh/config]

- link:
    ~/.dotfiles/.git/hooks: hooks
    ~/.aprc: aprc
    ~/.config/nvim: config/nvim
    ~/.gemrc: gemrc
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.railsrc: railsrc
    ~/.rbenv/default-gems: rbenv/default-gems
    ~/.rbenv/plugins: rbenv/plugins
    ~/.ssh: ssh
    ~/.tmux: tmux
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc

